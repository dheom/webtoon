class WebtoonModel {
  final String title, tumb, id;

WebtoonModel.fromJson(Map<String, dynamic> json)
    : title = json['title'],
      tumb = json['thumb'],
      id = json['id'];
  // Constructor to create a WebtoonModel object from JSON data
  }

  

